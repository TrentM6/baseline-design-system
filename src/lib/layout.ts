/**
 * Shared page layout constants — single source of truth so every workspace
 * uses identical margins/width. Change here, change everywhere.
 */
export const PAGE = "max-w-[1440px] mx-auto px-8 py-8";
